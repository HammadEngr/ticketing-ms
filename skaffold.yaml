apiVersion: skaffold/v2alpha3
kind: Config
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
  # tagPolicy:
  #   sha256: {}
  # local:
  #   push: false
  #   useBuildkit: true
  googleCloudBuild:
    projectId: tickets-503310

  artifacts:
    - image: us.gcr.io/tickets-503310/auth
      context: auth
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: "src/**/*.ts"
            dest: .
