apiVersion: skaffold/v2alpha3
kind: Config
deploy:
  kubectl:
    manifests:
      - ./infra/k8s/*
build:
  # tagPolicy:
  #   sha256: {}
  # local:
  #   push: false
  #   useBuildkit: true
  googleCloudBuild:
    projectId: ticketing-502622

  artifacts:
    - image: us.gcr.io/ticketing-502622/auth
      context: auth
      docker:
        dockerfile: Dockerfile
      sync:
        manual:
          - src: "src/**/*.ts"
            dest: .
